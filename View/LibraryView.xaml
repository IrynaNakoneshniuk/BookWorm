<UserControl x:Class="BookWorm.View.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookWorm.View"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="830">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Margin="10" Width="400" HorizontalAlignment="Left"
        Style="{StaticResource MaterialDesignOutlinedTextBox}"
        VerticalAlignment="Top"
        materialDesign:HintAssist.Hint="Введіть назву книги або автора"/>
        <!--<StackPanel  Orientation="Horizontal" Grid.Column="2" HorizontalAlignment="Right">
            <Menu  VerticalAlignment="Top"
                   Foreground="#36aab3">
                <MenuItem>
                    <MenuItem.Header >
                        <StackPanel Orientation="Horizontal" >
                            <TextBlock Text="Ірина" HorizontalAlignment="Center" Foreground="#36aab3" Margin="10"/>
                            <Image Source="C:\Users\ZbooK\source\repos\BookWorm\Assets\user.png" Width="40"/>
                        </StackPanel>
                    </MenuItem.Header>
                    <MenuItem Header="Увійти"/>
                    <Separator/>
                    <MenuItem Header="Вийти"/>
                </MenuItem>
            </Menu>
        </StackPanel>-->
        <Label Content="Бібліотека" VerticalAlignment="Bottom" FontWeight="Bold"
               Foreground="Orange" FontSize="25" Margin="10"/>
        <ListView Grid.Row="1" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="20">
                        <Border Background="White" Margin="20"  Height="200">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <Image></Image>
                                <TextBlock Grid.Row="1"></TextBlock>
                                <materialDesign:RatingBar Grid.Row="3"
                                x:Name="BasicRatingBar"
                                Value="3"/>
                                <Button
                                Style="{StaticResource MaterialDesignFlatLightButton}"
                                ToolTip="MaterialDesignFlatLightButton"
                                Content="Додати"
                                Width="{StaticResource ButtonWidth}"
                                IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />
                            </Grid>
                        </Border>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
